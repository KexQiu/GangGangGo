import { StyleSheet } from 'react-native';

import type { ThemeColors } from '../../../theme/colors';

export function createDataStyles(colors: ThemeColors) {
  return StyleSheet.create({
    header: { marginBottom: 18 },
    sectionHeader: {
      alignItems: 'flex-end',
      flexDirection: 'row',
      justifyContent: 'space-between',
      marginBottom: 10,
      marginTop: 4,
    },
    sectionCopy: { flex: 1, paddingRight: 12 },
    sectionTitle: { color: colors.text, fontSize: 18, fontWeight: '900', lineHeight: 24 },
    sectionCaption: { color: colors.textMuted, fontSize: 12, fontWeight: '600', lineHeight: 18, marginTop: 3 },
    privacyPill: {
      backgroundColor: colors.surfaceMuted,
      borderColor: colors.border,
      borderRadius: 999,
      borderWidth: 1,
      paddingHorizontal: 9,
      paddingVertical: 5,
    },
    privacyPillText: { color: colors.textMuted, fontSize: 10, fontWeight: '800' },
    todayCard: { marginBottom: 20, padding: 16 },
    todayTop: { alignItems: 'center', flexDirection: 'row', marginBottom: 13 },
    todayIcon: {
      alignItems: 'center',
      backgroundColor: colors.primarySoft,
      borderRadius: 18,
      height: 36,
      justifyContent: 'center',
      marginRight: 10,
      width: 36,
    },
    todayTitle: { color: colors.text, fontSize: 17, fontWeight: '900' },
    todayDate: { color: colors.textMuted, fontSize: 12, fontWeight: '700', marginTop: 2 },
    todayMetrics: { flexDirection: 'row', gap: 8 },
    metric: {
      backgroundColor: colors.surface,
      borderColor: colors.border,
      borderRadius: 15,
      borderWidth: 1,
      flex: 1,
      minWidth: 0,
      paddingHorizontal: 7,
      paddingVertical: 11,
    },
    metricValue: {
      color: colors.text,
      fontSize: 18,
      fontVariant: ['tabular-nums'],
      fontWeight: '900',
      textAlign: 'center',
    },
    metricLabel: { color: colors.textMuted, fontSize: 10, fontWeight: '800', marginTop: 4, textAlign: 'center' },
    metricHint: { color: colors.textSubtle, fontSize: 9, fontWeight: '700', marginTop: 4, textAlign: 'center' },
    gateCard: { alignItems: 'flex-start', gap: 8, marginBottom: 20 },
    gateTitle: { color: colors.text, fontSize: 16, fontWeight: '900' },
    gateText: { color: colors.textMuted, fontSize: 13, fontWeight: '600', lineHeight: 19 },
    chartCard: { marginBottom: 20, padding: 15 },
    segments: { backgroundColor: colors.surfaceMuted, borderRadius: 14, flexDirection: 'row', gap: 4, padding: 4 },
    segment: {
      alignItems: 'center',
      borderRadius: 10,
      flex: 1,
      justifyContent: 'center',
      minHeight: 34,
      paddingHorizontal: 6,
    },
    segmentActive: { backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1 },
    segmentDisabled: { opacity: 0.45 },
    segmentText: { color: colors.textMuted, fontSize: 12, fontWeight: '800' },
    segmentTextActive: { color: colors.text },
    categorySegments: { marginTop: 9 },
    chartValueRow: {
      alignItems: 'flex-end',
      flexDirection: 'row',
      height: 53,
      justifyContent: 'space-between',
      marginTop: 15,
    },
    chartValueCopy: { flex: 1, minWidth: 0, paddingRight: 8 },
    chartValue: {
      color: colors.text,
      fontSize: 25,
      fontVariant: ['tabular-nums'],
      fontWeight: '900',
      lineHeight: 32,
    },
    chartDate: { color: colors.textMuted, fontSize: 12, fontWeight: '700', lineHeight: 17, marginTop: 2 },
    chartDetailAction: {
      backgroundColor: colors.primarySoft,
      borderRadius: 999,
      paddingHorizontal: 10,
      paddingVertical: 6,
    },
    chartDetailActionText: { color: colors.primaryPressed, fontSize: 11, fontWeight: '800', lineHeight: 15 },
    chartCanvas: { height: 190, marginTop: 4, position: 'relative' },
    chartPressTarget: { flex: 1 },
    chartEmpty: {
      color: colors.textMuted,
      fontSize: 12,
      fontWeight: '700',
      left: 36,
      position: 'absolute',
      right: 12,
      textAlign: 'center',
      top: 78,
    },
    emptyText: {
      color: colors.textMuted,
      fontSize: 13,
      fontWeight: '600',
      lineHeight: 20,
      paddingVertical: 18,
      textAlign: 'center',
    },
    modalLoading: { alignItems: 'center', justifyContent: 'center', minHeight: 240 },
    modalContent: {
      gap: 16,
      paddingBottom: 34,
      paddingHorizontal: 20,
    },
    detailSummary: { flexDirection: 'row', gap: 8 },
    detailSummaryMetric: {
      backgroundColor: colors.surfaceMuted,
      borderRadius: 15,
      flex: 1,
      minWidth: 0,
      paddingHorizontal: 9,
      paddingVertical: 11,
    },
    detailSummaryLabelRow: { alignItems: 'center', flexDirection: 'row', gap: 5 },
    detailSummaryDot: { borderRadius: 3, height: 6, width: 6 },
    detailSummaryLabel: { color: colors.textMuted, flex: 1, fontSize: 10, fontWeight: '800' },
    detailSummaryValue: {
      color: colors.text,
      fontSize: 16,
      fontVariant: ['tabular-nums'],
      fontWeight: '900',
      marginTop: 7,
    },
    detailSection: { borderTopColor: colors.border, borderTopWidth: 1, gap: 9, paddingTop: 14 },
    detailTitle: { color: colors.text, fontSize: 15, fontWeight: '900' },
    detailRow: {
      alignItems: 'center',
      backgroundColor: colors.surfaceMuted,
      borderRadius: 13,
      flexDirection: 'row',
      gap: 9,
      paddingHorizontal: 12,
      paddingVertical: 10,
    },
    detailRowLabel: { color: colors.textMuted, flex: 1, fontSize: 13, fontWeight: '700' },
    detailRowValue: { color: colors.text, fontSize: 13, fontWeight: '900' },
    sessionCard: {
      backgroundColor: colors.surfaceMuted,
      borderColor: colors.border,
      borderRadius: 15,
      borderWidth: 1,
      gap: 7,
      padding: 12,
    },
    trainingSessionCard: {
      backgroundColor: colors.surfaceMuted,
      borderRadius: 15,
      gap: 7,
      padding: 12,
    },
    trainingSessionTitle: { flex: 1, paddingRight: 8 },
    trainingStatus: {
      backgroundColor: colors.primarySoft,
      borderRadius: 999,
      paddingHorizontal: 8,
      paddingVertical: 4,
    },
    trainingStatusIncomplete: { backgroundColor: colors.warningSoft },
    trainingStatusText: { color: colors.primaryPressed, fontSize: 10, fontWeight: '800' },
    trainingStatusTextIncomplete: { color: colors.warning, fontSize: 10, fontWeight: '800' },
    trainingSessionMeta: { color: colors.textMuted, fontSize: 12, fontWeight: '700' },
    trainingDiscomfort: { color: colors.danger, fontSize: 11, fontWeight: '800' },
    sessionHeader: { alignItems: 'center', flexDirection: 'row', justifyContent: 'space-between' },
    sessionTime: { color: colors.text, fontSize: 14, fontVariant: ['tabular-nums'], fontWeight: '900' },
    sessionDuration: { color: colors.primaryPressed, fontSize: 14, fontWeight: '900' },
    sessionToggle: { color: colors.textMuted, fontSize: 12, fontWeight: '800' },
    detailChips: { flexDirection: 'row', flexWrap: 'wrap', gap: 6 },
    detailChip: { backgroundColor: colors.surface, borderRadius: 9, paddingHorizontal: 7, paddingVertical: 4 },
    detailChipText: { color: colors.textMuted, fontSize: 10, fontWeight: '800' },
  });
}
