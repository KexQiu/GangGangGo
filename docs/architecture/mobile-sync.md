# 移动端同步

`SyncCoordinator` 是唯一自动同步入口。它在启动、登录变化、本地记录变化和回到前台时收集原因，750ms 防抖，并保证同一时间只有一轮任务运行；运行期间的新变化合并为一次尾随同步。

共享快照、90 天报告、Push 注册和 Watch 状态互不依赖时并行执行。任一云端失败不得回滚本地 mutation。服务端查询通过 TanStack Query key 去重，会话切换时清空缓存。
